# Frontend Environment Variables

# Backend API base URL (no /api suffix). Use same host:port as backend PORT.
# Example: if backend .env has PORT=3001, use http://localhost:3001
VITE_API_URL=http://localhost:3001

# Docker Environment Flag
# Set to 'true' when running in Docker
# Default: (not set)
DOCKER_ENV=

# Weather API Key (optional)
# Get your free API key from: https://www.weatherapi.com/signup.aspx
WEATHER_API_KEY=your_api_key_here
