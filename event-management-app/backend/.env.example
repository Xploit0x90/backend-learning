# Database (use DATABASE_URL or separate vars)
DATABASE_URL=postgresql://user:password@host:port/database
# Or: DB_HOST=localhost DB_PORT=5432 DB_USER=eventuser DB_PASSWORD=... DB_NAME=eventmanagement

PORT=5000

# Optional: Weather API (https://www.weatherapi.com/)
WEATHER_API_KEY=your_api_key_here

# Optional: CORS allowed origins (comma-separated). If unset, all origins allowed.
# CORS_ORIGIN=https://your-app.vercel.app,https://www.yourdomain.com
